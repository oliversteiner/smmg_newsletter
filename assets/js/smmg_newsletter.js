(function($, Drupal, drupalSettings) {
  Drupal.behaviors.smmgNewsletterBehavior = {
    attach(context, settings) {
      console.log("smmg Newsletter");
    },
  };
})(jQuery, Drupal, drupalSettings);
